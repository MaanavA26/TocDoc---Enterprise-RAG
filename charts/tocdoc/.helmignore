# Patterns to ignore when packaging the chart.
.DS_Store
.git/
.gitignore
*.swp
*.bak
*.tmp
*.orig
*~
# Local override values that may contain secrets — never package them.
secrets.yaml
*.secret.yaml
values.local.yaml
